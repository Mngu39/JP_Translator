{
  "name": "jp-translator-api",
  "private": true,
  "type": "module",
  "scripts": {
    "deploy": "wrangler deploy",
    "migrate": "wrangler d1 migrations apply jp_learning_log_db --remote",
    "dev": "wrangler dev"
  },
  "devDependencies": {
    "wrangler": "^4.30.0"
  }
}
